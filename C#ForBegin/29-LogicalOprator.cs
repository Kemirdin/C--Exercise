using System;

namespace SampleCodes
{
  class Program

  {
      static void  Main(string[] args)
      {
       int myAge = 25, minAge = 18, maxAge = 80;

       if (myAge >=minAge && myAge <= maxAge )
       Console.WriteLine ( "You can drive.");


      }
  }  
}